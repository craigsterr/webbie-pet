{
  "name": "web-pet-k",
  "homepage": "https://craigsterr.github.io/webbie-pet",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite --host",
    "preview": "vite preview",
    "zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\"",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "kaplay": "^3001.0.18"
  },
  "devDependencies": {
    "@types/node": "^22.15.23",
    "vite": "^6.3.5"
  }
}
