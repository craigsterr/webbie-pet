{
  "name": "web-pet-k",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\"",
    "dev": "vite --host"
  },
  "dependencies": {
    "kaplay": "^3001.0.18"
  },
  "devDependencies": {
    "@types/node": "^22.15.23",
    "vite": "^6.3.5"
  }
}
